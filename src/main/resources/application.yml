server:  servlet:    context-path: /csprscbw
spring:  datasource:     url: jdbc:postgresql://localhost:5432/LuiProfile
    #url: jdbc:postgresql://172.22.110.17:9876/obw
    username: postgres
    password: .2bcdjfz
    # type: com.zaxxer.hikari.HikariDataSource #當前預設的數據庫連接池
    driver-class-name: org.postgresql.Driver    hikari:      connection-timeout: 20000 # 客戶端從連接池等待連接的最大毫秒數      minimum-idle: 5 # 連接池中維護的最小連接數量      maximum-pool-size: 12 # 連接池最大數量      idle-timeout: 300000 # 允許一個連接池中的最大空閒時間(毫秒)      max-lifetime: 1200000 # 池中連結關閉後的最常生存時間(毫秒)      auto-commit: true # 配置從池中返回的默認自動提交行為  jpa:    hibernate:      ddl-auto: update    show-sql: false  #cache:    #cache-names: lui_cache    #jcache:      #config: classpath:ehcache.xmlmail:  transport:    protocol: smtp  smtp:    auth: true    starttls:      enable: true      required: true  debug: true  default-encoding: UTF-8  host: smtp.gmail.com  port: 587  username: t124650401@gmail.com  password: sbfsqoyivxoohdpe  domainip: localhost:8080codec:  key: csprscbw12345678  transformation: AES  algorithm: AES  tokenExpired: 86400000
